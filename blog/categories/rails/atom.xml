<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: rails | Bartj3]]></title>
  <link href="http://www.bartvanzon.com/blog/categories/rails/atom.xml" rel="self"/>
  <link href="http://www.bartvanzon.com/"/>
  <updated>2013-11-21T16:06:56+01:00</updated>
  <id>http://www.bartvanzon.com/</id>
  <author>
    <name><![CDATA[Bart van Zon]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Rails tree structure]]></title>
    <link href="http://www.bartvanzon.com/blog/2013/11/21/rails-tree-structure/"/>
    <updated>2013-11-21T15:56:00+01:00</updated>
    <id>http://www.bartvanzon.com/blog/2013/11/21/rails-tree-structure</id>
    <content type="html"><![CDATA[<p>A couple of days ago I had to make a documents/folder structure for an app. The
idea was that there&rsquo;s two types of objects: <strong>categories</strong> and <strong>documents</strong>,
the user can click through categories and download documents. A category can
contain other categories and documents. We only had to show the category the
user is currently in, the files and categories that are in that category and
some way to navigate one level higher. So there was no need to see children&rsquo;s
children etc.</p>

<!-- more -->


<p>A quick search on the <a href="https://www.ruby-toolbox.com">Ruby Toolbox</a> shows
<a href="https://github.com/stefankroes/ancestry">Ancestry</a> and a bunch of other options
but most of them are way to complicated for the simple goal I was trying to
achieve so I&rsquo;ve decided to build it from scratch.</p>

<p>I started out with two models, the Category model, and the Document model.</p>

<p>``` ruby app/models/category.rb
class Category &lt; ActiveRecord::Base
  belongs_to :parent, class_name: &lsquo;Category&rsquo;, foreign_key: &ldquo;parent_id&rdquo;
  validates :title, presence: true
  has_many :documents</p>

<p>  def children</p>

<pre><code>self.class.where(parent_id: id)
</code></pre>

<p>  end</p>

<p>  def parent_title</p>

<pre><code>parent.title if parent.present?
</code></pre>

<p>  end
end
```</p>

<p>The category can belong to other categories, and that optional parent is stored
in the <code>parent_id</code> variable. A category has a title as we need something to show
when listing the categories and a category can have several documents in it.</p>

<p><code>ruby app/models/document.rb
class Document &lt; ActiveRecord::Base
  validates :title, presence: true
  belongs_to :category
end
</code></p>

<p>Documents have a title to display them (and in a real world application probably
a file to go with it) and it may belong to a category. If it has no category it
should be displayed on the top level of the tree.</p>

<p>``` ruby db/schema.rb
create_table &ldquo;categories&rdquo;, force: true do |t|
  t.string   &ldquo;title&rdquo;
  t.integer  &ldquo;parent_id&rdquo;
end</p>

<p>create_table &ldquo;documents&rdquo;, force: true do |t|
  t.string   &ldquo;title&rdquo;
  t.integer  &ldquo;category_id&rdquo;
end
```
The database schema should look something like this.</p>

<p>One of the problems with these models is that a category can reference to ANY
category as it&rsquo;s parent. So even itself, to prevent that we&rsquo;ll need a custom
validator, it can be added to the category model with the following line:</p>

<p><code>ruby app/models/category.rb
validates_with CategoryParentValidator
</code></p>

<p>And the validator itself would look something like this:
``` ruby app/validators/category_parent_validator.rb
Class CategoryParentValidator &lt; ActiveModel::Validator
  def validate(record)</p>

<pre><code>if !record.new_record? &amp;&amp; record.parent_id == record.id
  record.errors[:parent] &lt;&lt; I18n.t('cant_be_itself', scope: 'errors.messages')
end
</code></pre>

<p>  end
end
```
If the record is new we shouldn&rsquo;t check if the parent_id is the same as the
current id because the current id will be nil. So a new record without a parent
would have both id&rsquo;s on nil but is still valid.</p>

<p>The next step in building the tree structure is the controller:
``` ruby app/controllers/documents_controller.rb
class DocumentsController &lt; ApplicationController
  before_action :set_document, only: [:show]
  before_action :set_files_variables, only: [:index, :show]</p>

<p>  def index
  end</p>

<p>  def show
  end</p>

<p>  private
  def set_document</p>

<pre><code>@document = Document.find params[:id]
</code></pre>

<p>  end</p>

<p>  def set_files_variables</p>

<pre><code>@selected_category = params[:selected_category]
@parent_category = get_parent_category_id(@selected_category)
@categories = Category.where(parent_id: @selected_category)
@documents = Document.where(category_id: @selected_category)
</code></pre>

<p>  end</p>

<p>  def get_parent_category_id(category_id)</p>

<pre><code>begin
  selected_category = Category.find(category_id)
  return selected_category.parent.id if selected_category.parent.present?
rescue ActiveRecord::RecordNotFound
end

nil #the category was not found or there was no parent
</code></pre>

<p>  end
end
```
There&rsquo;s a bunch of variables we&rsquo;re going to need in the view, the selected
category is needed so we can show the title of the category we&rsquo;re currently in.
The parent_category is needed to navigate to one level higher, the categories
are the categories in the selected category and the documents are the documents
in the selected category.</p>

<p>``` erb app/views/documents/_document_list.html.erb</p>

<h3>Document List</h3>


<ul id="document_list">
  <% if @selected_category.present? %>
    <li class="parent">
    <%= link_to '..', documents_path(selected_category: @parent_category) %>
    </li>
  <% end %>
<% @categories.each do |category| %>
  <li class="category">
    <%= link_to "#{category.title}", documents_path(selected_category: category.id) %>
  </li>
<% end %>

<% @documents.each do |document| %>
  <li class="document <%= document_icon_class(document) %>">
    <%= link_to "#{document.title}", document_path(document, selected_category: @selected_category) %>
  </li>
<% end %>
</ul>


<p>```
I expected the view to be pretty complex but it turned out to be easier then I
expected. We show the selected category, a way of navigating and a list of
categories and files in the category. Because there&rsquo;s no need to see children&rsquo;s
children this stays pretty simple.</p>
]]></content>
  </entry>
  
</feed>
